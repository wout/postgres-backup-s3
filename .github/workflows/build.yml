name: build

on:
  push:
    branches: [main]
    tags: ['v*']
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v7

      - uses: docker/setup-qemu-action@v4

      - uses: docker/setup-buildx-action@v4

      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - id: meta
        uses: docker/metadata-action@v6
        with:
          images: ghcr.io/${{ github.repository }}
          tags: |
            type=ref,event=branch
            type=ref,event=tag
            type=sha,format=short
            type=raw,value=latest,enable={{is_default_branch}}

      - uses: docker/build-push-action@v7
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # Trigger a redeploy of every CapRover app that runs this image.
  # Each app in CapRover has a per-app webhook URL under
  #   App > Deployment > "Method 5: Deploy plain Dockerfile".
  # Put those URLs (comma-separated) into a repo secret named
  # CAPROVER_WEBHOOKS, then uncomment this job.
  #
  # notify:
  #   needs: build
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Ping CapRover webhooks
  #       env:
  #         WEBHOOKS: ${{ secrets.CAPROVER_WEBHOOKS }}
  #       run: |
  #         IFS=',' read -ra urls <<< "$WEBHOOKS"
  #         for url in "${urls[@]}"; do
  #           echo "pinging $url"
  #           curl -fsSL -X POST "$url"
  #         done
